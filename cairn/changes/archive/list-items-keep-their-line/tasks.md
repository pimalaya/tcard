---
cairn: tasks
change: list-items-keep-their-line
---

# Tasks

- [x] Match each item back to the line whose value held it, rather than counting off the front.
- [x] Give the leftover items one new line between them instead of one line each.
- [x] Treat a property holding at most one line as that line, in document order.
- [x] Match on the values rather than on their escaping, escaping only on the way out.
- [x] Cover the removal, the rename, the join, the shared new line and `ORG` with tests.
- [x] Fold the delta into the spec and write the log entry.
