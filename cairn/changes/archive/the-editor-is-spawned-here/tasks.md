---
cairn: tasks
change: the-editor-is-spawned-here
---

# Tasks

- [x] Write the spawn: temporary file, inherited streams, read back, in cli/editor.rs.
- [x] Resolve `$VISUAL` then `$EDITOR`, bailing with both names and the flag.
- [x] Add `--editor <COMMAND>` as a shared argument on `edit` and `merge`.
- [x] Keep and name the buffer when a fold-back is declined.
- [x] Drop the `edit` dependency and its feature entry.
- [x] Document it in the README, the crate header and CHANGELOG.md.
- [x] Fold the delta into the spec and write the log entry.
